RSL-plus 1.1.4 Fast Open & Performance Patch

Накладывается на текущий master после resource-optimization patch.
Версия package.json остаётся 1.1.4.

Изменённые области:
- immediate open parse / debounced edits;
- shared lexer/parser snapshot for Folding and Outline;
- two-phase local/workspace diagnostics with max wait;
- external module summary scanner and stored definition ranges;
- single bounded ImportContext cache;
- semantic tokens full/delta/range;
- lightweight transient References context;
- delayed workspace inventory and throttled status updates.

После распаковки обязательно выполнить:
  powershell -ExecutionPolicy Bypass -File .\apply-performance-patch.ps1
  npm.cmd test
  vsce.cmd package
